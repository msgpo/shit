# Service only works when certain user is logged in (Systemd)
----------------------------------------------------------
systemd services when started with the `--user` flag
only are ran whilst the given user is logged in
